import Header from "./Header/Header";
import AppRouter from "../route/RouterComponent";

const Layout = () => {
  return (
    <div>
      <Header />
      <AppRouter />
    </div>
  );
};

export default Layout;
